const router = require('express').router();

const logger = require("@core/logger")('app.routes.{{ camelCase name }}');
const {{ camelCase name}}Controller = require('@controller/{{ camelCase name }}');

{{#if wantMiddleware}}
const {{ camelCase name }}Middleware = require('@middlewares/{{ camelCase name}}');
{{/if}}

{{#if wantValidation}}
const validate = require('@middlewares/validate');
const {{ camelCase name }}Validator = require('@validations/{{ camelCase name}}');
{{/if}}


router.route('/').get();

module.exports = router;